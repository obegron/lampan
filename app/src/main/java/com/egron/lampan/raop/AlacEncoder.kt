package com.egron.lampan.raop

class AlacEncoder {
    fun encode(pcm: ByteArray): ByteArray {
        // Minimal stub: pass-through PCM for now.
        // Real ALAC encoding would go here.
        return pcm
    }
}
